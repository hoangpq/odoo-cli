#!/usr/bin/env node

require('commander')
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('module', 'generate a new module from a template')
  .parse(process.argv)
